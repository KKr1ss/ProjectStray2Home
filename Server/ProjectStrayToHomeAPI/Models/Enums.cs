namespace ProjectStray2HomeAPI.Models
{
    public enum Roles { User, Admin }
    public enum Sex { Male = 0, Female = 1 }
    public enum Animal_Type { Cat = 0, Dog = 1 }
    public enum Animal_Status { Home = 0, Lost = 1, Rescued = 2,  Stray = 3}
}
